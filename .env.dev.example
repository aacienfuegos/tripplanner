# ── Base de datos ─────────────────────────────────────────────────────────────
# El host es "db" (nombre del servicio en docker-compose), no localhost
DATABASE_URL="postgresql://tripuser:tripdev123@db:5432/tripplanner_dev"
DB_USER=tripuser
DB_PASSWORD=tripdev123
DB_NAME=tripplanner_dev

# ── NextAuth ──────────────────────────────────────────────────────────────────
AUTH_SECRET="dev-secret-no-usar-en-produccion"
AUTH_URL="http://localhost:3000"

# ── Puerto del host donde se expondrá la app ──────────────────────────────────
APP_PORT=3000

# ── Credenciales de acceso dev (hardcoded en auth.ts) ────────────────────────
# Email:    admin@dev.local
# Password: admin123

# ── Admin ─────────────────────────────────────────────────────────────────────
ADMIN_EMAIL="admin@dev.local"
